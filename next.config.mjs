/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    images: {
      unoptimized: true,
    },
    basePath: '/tanishk',
    assetPrefix: '/tanishk',
  };
  
  export default nextConfig;
  